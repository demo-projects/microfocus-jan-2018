import { ClickAndStopDirective } from './click-and-stop.directive';

describe('ClickAndStopDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickAndStopDirective();
    expect(directive).toBeTruthy();
  });
});
